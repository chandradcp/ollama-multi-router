node_modules/
.env
logs/
server.log

# Claude Code session data — local only
.claude/

# Contains real Ollama API keys — keep it local only
config/accounts.json

# Contains the generated local API key — keep it local only
config/gateway.json

# Contains client (tenant) API keys — keep it local only
config/clients.json

# Runtime per-account usage counters
config/usage.json

# macOS
.DS_Store
