services:
  ollama-multi-router:
    build: .
    ports:
      - "20128:20128"
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
    env_file:
      - .env
    restart: unless-stopped
